
  // Fetch and print all commands
  // client.application.commands.fetch()
  // .then(commands => {
  //   console.log(`Fetched ${commands.size} commands`);
  //   console.log(commands);
  // })
  // .catch(console.error);